<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#00BEEE</item>
        <item name="colorPrimaryDark">#211F25</item>
        <item name="colorAccent">#C7D3D2</item>
    </style>

    <style name="CustomSwitchTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Active thumb color & Active track color(30% transparency) -->
        <item name="colorControlActivated">@color/sus_green_medium</item>
        <!-- Inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/sus_green</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">#757575</item>
    </style>
    
    <style name="MyProgressBar" parent="@style/Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressBackgroundTint">@color/sus_green_medium</item>
        <item name="android:progressTint">@color/sus_green_dark</item>
        <item name="android:scaleY">1.4</item>
        <item name="android:max">31</item>
        <item name="android:layout_width">280dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">30dp</item>
        <item name="android:layout_marginBottom">-40dp</item>
    </style>

    <style name="ProgressBarText" parent="@style/Widget.AppCompat.TextView">
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@color/text_black</item>
        <item name="android:layout_marginLeft">340dp</item>
    </style>

    <style name="ProgressBarOverText" parent="@style/Widget.AppCompat.TextView">
        <item name="android:textSize">13dp</item>
        <item name="android:textColor">@color/text_black</item>
        <item name="android:layout_marginTop">-18dp</item>
    </style>

    <style name="ProgressBarIcon" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:scaleY">1</item>
        <item name="android:layout_height">28dp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:layout_marginLeft">360dp</item>
    </style>
    
</resources>